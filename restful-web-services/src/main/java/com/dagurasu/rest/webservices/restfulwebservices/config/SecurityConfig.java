package com.dagurasu.rest.webservices.restfulwebservices.config;

//@EnableWebSecurity
public class SecurityConfig {

	/*
	 * @Bean public SecurityFilterChain securityFilterChain(HttpSecurity http)
	 * throws Exception {
	 * 
	 * http.csrf() .disable() .authorizeRequests() .antMatchers("/h2-console/**")
	 * .permitAll() .mvcMatchers(HttpMethod.GET, "/users") .permitAll()
	 * .anyRequest() .authenticated() .and() .httpBasic();
	 * 
	 * http.headers() .frameOptions() .disable();
	 * 
	 * return http.build(); }spring.jpa.defer-datasource-initialization=true
	 */
	
}
